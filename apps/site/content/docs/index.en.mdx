---
title: "Documentation"
description: "Every guide and reference page for @jaeungkim/gantt-chart, in reading order."
---

Read in order if you are new. Jump straight to a guide if you are not.

## Guides

| Page | What it covers |
|---|---|
| [Introduction](introduction) | What the library is, and what it deliberately leaves to you |
| [Concepts and vocabulary](concepts) | Every term the rest of the docs use, defined once |
| [Quick start](quick-start) | Install to a working, editable chart |
| [Task data](task-data) | The `Task` shape, date handling, and how the `tasks` prop is compared |
| [Task list and hierarchy](task-list) | The left pane and the `parentId` tree |
| [Lanes](lanes) | The `lane` task field, which packs tasks that never overlap onto one row |
| [The timeline](timeline) | Scales, range, zoom, the today line, and non-working days |
| [Editing tasks](editing) | Move, resize, progress, permissions, drag bounds, touch, and auto-scrolling |
| [Creating tasks](task-creation) | The three ways a new task is proposed, the draft they all hand to `onTaskCreate`, and why the chart never adds the row itself |
| [Reordering rows](reordering) | Dragging a task-list row to a new position or a new parent, what the move rewrites, and how to reject one |
| [Dependencies](dependencies) | The four link types, and drawing arrows |
| [Working calendar](working-calendar) | Keeping a drop off weekends and holidays |
| [Detail panel](detail-panel) | The docked side panel: what opens it, editing in place, `renderDetail`, and controlling it |
| [Events](events) | Every callback a click, a drag and a keyboard edit fire |
| [Styling](styling) | Per-task color and class names |
| [Imperative API](imperative-api) | The `ref` handle: scrolling, zoom, the detail panel and task creation |
| [Keyboard and screen readers](accessibility) | The key map, the ARIA tree, and the gaps |
| [Locale and date formats](i18n) | `locale`, per-scale overrides, week start |
| [Theming](theming) | The theme prop and the CSS custom properties |
| [Headless core](headless-core) | Tree and calendar math without React or a DOM |

## Reference

| Page | What it covers |
|---|---|
| [GanttProps](ref/props) | every prop the component accepts |
| [Task and task types](ref/task) | `Task`, `TaskDependency`, `DependencyType`, `TaskTransformed` |
| [GanttInteractionConfig](ref/interaction-config) | `GanttInteractionConfig` and the capability order |
| [GanttRow](ref/rows) | The row model the chart builds from your tasks |
| [Detail renderer](ref/renderers) | `GanttDetailRenderer` and `GanttDetailRenderProps`, the types behind `renderDetail` |
| [GanttHandle](ref/handle) | `GanttHandle`, `GanttScrollApi`, `GanttDetailApi`, `GanttTaskCreateApi`, `GanttScrollOptions`, `GanttZoomAnchor` |
| [Scale and theme types](ref/scales) | `GanttScaleKey`, `GanttScaleFormat`, `GanttFormatOverrides`, `GanttTheme`, `Holiday` |
| [Tree helpers](ref/core-tree) | `buildTaskTree`, `collectSubtreeIds`, `rollUpTasks`, `moveTask`, `validateMove`, `sortTasksBySequence` |
| [Working calendar helpers](ref/core-calendar) | The exported calendar API: `createWorkingCalendar`, `CALENDAR_DAYS`, `WorkingCalendar` |
